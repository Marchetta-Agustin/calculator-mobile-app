import ScreenContainer from "@/components/ScreenContainer";
import { View, Text } from "react-native";

const GlobalScreen = () => {
    return(

        <ScreenContainer>
            <Text>Monedas Globales</Text>
        </ScreenContainer>
    );
};

export default GlobalScreen;